# Required. Must be at least 32 characters — seals the session cookie.
# Generate one with: node -e "console.log(crypto.randomBytes(32).toString('hex'))"
NUXT_SESSION_PASSWORD=

# Optional. Where the SQLite file lives. Defaults to .data/record-browser.db
NUXT_DATABASE_PATH=.data/record-browser.db
